{
    "play_success_message_template": {
    "type": "string",
    "description": "自定义点歌成功时的回复",
    "hint": "支持使用变量：{title}（歌名）、{artist}（歌手），例如：已为你播放《{title}》--{artist}，留空则不回复",
    "default": "🎵已为您播放《{title}》"
  }
}